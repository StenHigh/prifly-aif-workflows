authoring: prifly-step/1
id: aif:step/fanout-choose
version: 1.0.0
title: Let the developer choose fan-out suggestions
kind: worker
inputs:
  proposals: {schema_ref: "{{aggregate_manifest_schema}}"}
outputs:
  selection: {schema_ref: "{{schema_selection}}", required_for: [pass]}
executor: {adapter_ref: "{{assisted_adapter}}", operation: session}
instructions_ref: "{{context_fanout-select}}"
effects: {class: none, retry_class: never}
result_schema_ref: "{{step_result_schema}}"
