authoring: prifly-step/2
id: aif:step/fanout-improve
version: 1.1.0
title: Ask one independent plan-review perspective
kind: worker
inputs:
  plan: {schema_ref: "{{schema_plan}}"}
  profile: {schema_ref: "{{schema_profile}}"}
outputs:
  proposal: {schema_ref: "{{schema_proposal}}", required_for: [pass, needs_revision]}
executor: {adapter_ref: "{{assisted_adapter}}", operation: session}
instructions_ref: "{{context_aif-improve}}"
effects: {class: none, retry_class: never}
session_limits: {active_timeout_ms: 2592000000}
result_schema_ref: "{{step_result_schema}}"
