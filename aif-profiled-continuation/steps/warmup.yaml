authoring: prifly-step/2
id: aif-profiled-continuation:step/warmup
version: 1.7.0
title: Run aif-warmup and hand its compact reading forward
kind: worker
outputs:
  handoff: {schema_ref: "{{schema_warmup-handoff}}", required_for: [pass]}
executor: {adapter_ref: "{{assisted_adapter}}", operation: session}
instructions_ref: "{{context_aif-warmup-bridge}}"
context_refs: ["{{context_aif-warmup}}"]
effects: {class: none, retry_class: pure}
session_limits: {active_timeout_ms: null}
result_schema_ref: "{{step_result_schema}}"
model_profile:
  requested: fast-draft
  reason: >-
    Warmup reads the project and distils a handoff: breadth and speed, not depth. Its product is a pointer list, not a judgement.
